extends layout 

block content 
    h1(class='mb-4')= title 
    if category_list.length
        ul
            each category in category_list 
                li
                    a(href=category.url) #{category.name}
    else
        p No hay Categorías. 